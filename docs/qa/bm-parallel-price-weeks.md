# Паралельний прохід Booking Manager у нічній задачі price-weeks

Прогрес: 4 з 6

| #   | Крок                                                    | Статус   | Нотатки                                                                                                                                  |
| --- | ------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Читання коду і план                                     | зроблено | `orderedWindow` і `BOOKING_MANAGER_SWEEP_CONCURRENCY` вже є; прохід price-weeks позичає ліміт каталогу (12) і не має власного регулятора |
| 2   | Env + config: `BOOKING_MANAGER_PRICE_WEEKS_CONCURRENCY` | зроблено | типово 4, межі 1..8, `pnpm check-types` чистий                                                                                           |
| 3   | Регулятор паралельності і зниження після 429/5xx        | зроблено | `createConcurrencyGovernor`, ширина читається перед кожним запуском                                                                      |
| 4   | Юніт-тести                                              | зроблено | 949 тестів `@yacht-charter/providers` зелені; нові файли `concurrency-governor.test.ts`, `price-weeks-concurrency.test.ts`               |
| 5   | Живе вимірювання, PRICE_WEEKS_COUNT=4                   | у роботі |                                                                                                                                          |
| 6   | Документація і перевірки                                | -        |                                                                                                                                          |

## Що вже було в коді

- `packages/providers/src/shared/ordered-window.ts` вже запускає N запитів одночасно і віддає відповіді строго в порядку вхідного списку.
- `streamBookingManagerConfirmedOffers` уже користується ним із шириною `config.sweepConcurrency` (типово 12), тобто прохід price-weeks для Booking Manager фактично вже був паралельним, але з лімітом, підібраним для каталогу, і без реакції на 429.
- NauSYS іде окремим шляхом (`nausys/provider.ts`) і лишається строго послідовним.
