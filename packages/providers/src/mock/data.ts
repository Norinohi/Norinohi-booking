import catalogueFixture from "./fixtures/catalogue.json" with { type: "json" };
import availabilityFixture from "./fixtures/availability.json" with { type: "json" };

export const catalogue = catalogueFixture;
export const availability = availabilityFixture;
