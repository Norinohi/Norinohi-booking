{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm turbo run build --filter=server...",
    "watchPatterns": [
      "apps/server/**",
      "packages/api/**",
      "packages/auth/**",
      "packages/config/**",
      "packages/db/**",
      "packages/env/**",
      "package.json",
      "pnpm-lock.yaml",
      "pnpm-workspace.yaml",
      "turbo.json",
      ".node-version"
    ]
  },
  "deploy": {
    "preDeployCommand": "pnpm --filter server migrate",
    "startCommand": "pnpm --filter server start",
    "healthcheckPath": "/",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
